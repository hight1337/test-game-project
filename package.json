{
  "name": "f1web",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run typecheck && npm run build -w client",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server",
    "start:server": "npm run start -w server",
    "fetch-tracks": "node tools/fetch-tracks.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
